# Create a custom GitHub Workflow

* Create a reusable workflow to use your action to add custom properties to a docker container
* Workflow should at tall the custom properties as key/value pair labels to container
* Workflow should utilize existing workflows when possible
* Build and deploy your workflow using our reusable workflows
* Create unit and integration tests for the workflow
